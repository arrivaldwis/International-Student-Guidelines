<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sandywinata.isg">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.Login" />
        <activity android:name=".view.activity.Home" />
        <activity android:name=".view.activity.Attraction" />
        <activity android:name=".view.activity.BITMaps" />
        <activity android:name=".view.activity.Conversation" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.activity.BITGMaps"
            android:label="@string/title_activity_bitgmaps" />
        <activity android:name=".view.activity.WelcomeToBit" />
        <activity android:name=".view.activity.IntroductionOffice" />
        <activity android:name=".view.activity.ZhongguancunCampus" />
        <activity android:name=".view.activity.StartingSemester" />
        <activity android:name=".view.activity.Undergraduate" />
        <activity android:name=".view.activity.BITCampuses" />
        <activity android:name=".view.activity.ListProgram" />
        <activity android:name=".view.activity.Startingday" />
        <activity android:name=".view.activity.Contacts" />
        <activity android:name=".view.activity.Greetings" />
        <activity android:name=".view.activity.LifeStyle" />
        <activity android:name=".view.activity.Chinese" />
        <activity android:name=".view.activity.Pointofinterest" />
        <activity android:name=".view.activity.TodoList" />
        <activity android:name=".view.activity.Academic" />
        <activity android:name=".view.activity.Visa" />
        <activity android:name=".view.activity.Registration" />
        <activity android:name=".view.activity.Transcript" />
        <activity android:name=".view.activity.Accomodation" />
        <activity android:name=".view.activity.Askforleave" />
        <activity android:name=".view.activity.DetailAccomodation" />
        <activity
            android:name=".view.activity.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.ChatActivity"></activity>
    </application>

</manifest>